// Prerender the entire app at build time.
// The server load functions run once during `vite build`, fetching live FPL data.
// The resulting static files are deployed to GitHub Pages.
export const prerender = true;
